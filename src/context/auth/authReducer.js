import {
  SINGUP_SUCCESS,
  SINGUP_ERROR,
  FIND_USER,
  LOGIN_SUCCESS,
  LOGIN_ERROR,
  LOG_OUT
} from "../../types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
